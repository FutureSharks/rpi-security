#!/bin/bash

# install raspistill, python-picamera, netaddr
# start service
# pip install pushbullet.py picamera urllib3 requests[security], netifaces netaddr, scappy, 
# sudo apt-get install python-dev
